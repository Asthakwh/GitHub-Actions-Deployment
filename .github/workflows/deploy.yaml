name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Debug files
        run: ls -la

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
